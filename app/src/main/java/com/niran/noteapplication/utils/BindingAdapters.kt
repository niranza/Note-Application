package com.niran.noteapplication

/*
@BindingAdapter("handleStatus")
fun onButtonClick(button: Button, status: NoteViewModel.ClickState) {

    val context = button.context
    val snackPos: View = button.rootView.findViewById(R.id.snack_pos)

    when (status) {
        NoteViewModel.ClickState.NOTIFY -> {
            AppUtils.showSnackBar(snackPos, R.string.notifying_mode)
            button.text = context.getString(R.string.delete_notes)
        }
        else -> {
            AppUtils.showSnackBar(snackPos, R.string.deleting_mode)
            button.text = context.getString(R.string.remind_me)
        }
    }
}*/
